#!/usr/bin/env bash

# Verify if a program exists
# [[ $(type -P Program ) ]] || { echo "The program is necessary !" ; exit 1 ;}

# Verify internet
# wget -q --spider www.google.com

# Verify if is root 
# [[ "$UID" -ne 0 ]] && { echo "root is necessary to run. " ; exit 1 ;}


